#include <iostream>
#include <string.h>
#include <fstream>

using namespace std;
int main() {
    string stream ss;
    bool is EOF = ss.eof();
    cout<<"is stream eof: "
        <<"is EOF"<<endl;
    return 0;
}
